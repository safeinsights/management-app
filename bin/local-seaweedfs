#!/bin/sh

# SeaweedFS must be installed first: brew install seaweedfs

DIR="./tmp/local-s3"; [ -d "$DIR" ] || mkdir -p "$DIR"

AWS_ACCESS_KEY_ID=si-local-s3 AWS_SECRET_ACCESS_KEY=si-local-s3 weed server -s3 -dir="$DIR" -s3.port=8333
