#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if [ "${E2E_MODE:-}" != "docker" ]; then
    echo "docker-e2e-app must be started by ./bin/docker-e2e" >&2
    exit 1
fi

exec ./bin/e2e-app-server "$@"
